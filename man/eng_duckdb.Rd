% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine.R
\name{eng_duckdb}
\alias{eng_duckdb}
\title{Knitr engine for DuckDB CLI}
\usage{
eng_duckdb(options)
}
\arguments{
\item{options}{A list of chunk options provided by knitr.}
}
\value{
A character string of formatted output for knitr.
}
\description{
Knitr engine for DuckDB CLI
}
\details{
Supported chunk options:
\describe{
\item{\code{db}}{Path to a DuckDB database file. Defaults to \code{":memory:"}.}
\item{\code{session}}{Session name. If it already exists, it is reused.
Otherwise a new session is created. When omitted, the last used session
is reused (or a new one is created automatically).}
\item{\code{mode}}{DuckDB output mode (e.g., \code{"table"}, \code{"markdown"}, \code{"csv"}).
Sends \verb{.mode <value>} before executing the chunk's SQL.}
\item{\code{timeout}}{Maximum milliseconds to wait for output. Defaults to
\code{30000}.}
\item{\code{ansi}}{Controls handling of ANSI escape codes in output.
\code{TRUE} preserves them, \code{FALSE} (default) strips them, and \code{"html"}
converts them to HTML markup via \code{\link[cli:ansi_html]{cli::ansi_html()}}.}
}
}
