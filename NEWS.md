# duckknit 0.0.9.9000

* Initial release.
* `{duckdb}` knitr engine with persistent CLI sessions across chunks.
* Chunk options: `db`, `session`, `mode`, `timeout`, `ansi`.
* `duckdb_sitrep()` for diagnostics.
