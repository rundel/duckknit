# DuckDB CLI situation report

Reports the status of the DuckDB CLI tool including its location,
version, and any active sessions.

## Usage

``` r
duckdb_sitrep()
```

## Value

Invisibly returns a list with `path`, `version`, and `sessions`.
